# Environment Configuration
# Copy this file to .env and fill in your values

# === LLM Provider Configuration ===
# The system will auto-detect which API key is available and use that provider
# Priority: Google Gemini (if GOOGLE_API_KEY set) → OpenAI (if OPENAI_API_KEY set) → Rule-based fallback

# Google Gemini API Key (recommended - has free tier with 60 req/min, Pro subscription gets higher limits)
GOOGLE_API_KEY=your_google_api_key_here
GEMINI_MODEL=gemini-1.5-flash  # Options: gemini-pro, gemini-1.5-flash, gemini-1.5-pro

# OpenAI API Key (alternative - requires payment, but gpt-4o-mini is very cheap)
OPENAI_API_KEY=your_openai_api_key_here
LLM_MODEL=gpt-4o-mini  # Options: gpt-4o-mini, gpt-4o, gpt-4

# Anthropic API Key (not yet implemented, but shown for reference)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# === Whisper Model Configuration ===
WHISPER_MODEL=base  # Options: tiny, base, small, medium, large (larger = better accuracy but slower)

# === Streaming Configuration ===
CHUNK_DURATION_SECONDS=5  # Duration of audio chunks for processing (in seconds)
STREAMING_PORT=8000  # Port for FastAPI server
